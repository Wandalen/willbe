about :

    name : predefinedSteps
    description : "To use predefined submodule control steps"
    version : 0.0.1

submodule :

    Tools : git+hd://../repo/Tools?out=out/wTools.out.will#master
    PathFundamentals : git+hd://../repo/PathFundamentals?out=out/wPathBasic.out.will#master
    # Tools : git+https:///github.com/Wandalen/wTools.git/out/wTools.out.will#master
    # PathFundamentals : git+https:///github.com/Wandalen/wPathBasic.git/out/wPathBasic.out.will#master

step :

    echo :
        shell: echo "Done"

build :

    download :
        steps :
           - submodules.download

    upgrade :
        steps :
           - submodules.update

    clean :
        steps :
           - submodules.clean

    build :
        criterion :
          default : 1
        steps :
           - submodules.clean
           - submodules.download
           - echo
