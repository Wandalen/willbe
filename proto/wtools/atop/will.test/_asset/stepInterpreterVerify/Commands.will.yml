about :

  name : 'stepInterpreterVerify'

step :

  interpreter.and.command :
    inherit : interpreter.verify
    interpreters :
      npm : '>= 3.0.0 AND <= 30.0.0'
    commands :
      npm : 'npm --version'

build :

  interpreter.and.command :
    steps :
      - step::interpreter.and.command
